# 2026-05-25 工作记录

## MyoSign 项目 — 首次全面分析

### 完成事项
1. **读取并分析了全部项目文件**:
   - MyoSign.pptx, dronegrounder.pptx (另一个项目)
   - 原理图 SCH_Schematic1_2026-05-25.pdf (2页)
   - PCB PCB_PCB1_2026-05-25.pdf (7层)
   - BOM 表 (51种元器件, 全部 LCSC 来源)
   - PickAndPlace 贴片坐标
   - Core1106 引脚定义 (113脚, RV1106 G2)
   - 肌电数据 CSV (EMG_2Chs, 6种手势)
   - ML 代码 (NN/CNN/KNN/SVM/RF/DT, AE 自编码器)

2. **多次修正架构理解**:
   - 修正1: 从 INMP441 数字麦克风 → 模拟麦克风
   - 修正2: 从 ESP32-C3 存在 → 只有 ESP32-S3
   - 修正3: 从双向 UART → 单向 ESP32→Core1106
   - 修正4: 去掉 IMU, 去掉数字人手语, 用户只管硬件不管模型

3. **创建了完整的 ESP32-S3 嵌入式项目** (`esp32/`):
   - README.md: 超详细开发文档 (约500行)
   - config.h: 全局配置 (引脚/波特率/队列)
   - main.c: 程序入口 (创建队列+任务)
   - ble_recv.c: NimBLE GATT Server (接收采集端肌电数据)
   - uart_tx.c: UART 单向发送 (打包协议帧→Core1106)
   - protocol.c: 通信协议 (CRC8查表法, 帧打包/验证)
   - CMakeLists.txt + sdkconfig.defaults: 构建配置

### 关键待确认
- UART RX3/TX3 对应 ESP32-S3 的哪个 GPIO (config.h 中标注了 `⚠ 待确认`)
- 采集端 BLE 协议需要对齐 (Service UUID, Characteristic UUID, 数据格式)

### 技术决策
- 框架: ESP-IDF v5.x (不用 Arduino)
- BLE 栈: NimBLE (不用 Bluedroid)
- 裸 FreeRTOS 多任务 (2 个任务)
- 串口协议: 帧头尾包夹 + CRC8
